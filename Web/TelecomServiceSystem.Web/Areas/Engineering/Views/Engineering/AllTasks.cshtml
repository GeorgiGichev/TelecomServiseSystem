@model IEnumerable<TelecomServiceSystem.Web.ViewModels.Tasks.TaskViewModel>

@{ this.ViewData["Title"] = "All tasks"; }

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-12">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header"><h3 class="text-center font-weight-light my-4">All tasks</h3></div>
                <div class="card-body">
                    <div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-bordered table-striped" id="dataTable" width="100%" cellspacing="0">
                                    <thead>
                                        <tr>
                                            <th>Task Id</th>
                                            <th>Instalation Date</th>
                                            <th>Team</th>
                                        </tr>
                                    </thead>
                                    <tfoot>
                                        <tr>
                                            <th>Task Id</th>
                                            <th>Instalation Date</th>
                                            <th>Team</th>
                                        </tr>
                                    </tfoot>
                                    <tbody>
                                        @foreach (var task in Model)
                                        {
                                         <tr>
                                             <td><a asp-area="Engineering" asp-controller="Engineering" asp-action="InstalationInfo" asp-route-orderId="@task.OrderId">@task.Id</a></td>
                                             <td>@task.InstalationDate</td>
                                             <td>@task.TeamName</td>
                                         </tr>}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                </div>
                <div class="col-md-6 col-md-offset-2">
                    <section>
                    </section>
                </div>
            </div>
        </div>
    </div>
</div>
